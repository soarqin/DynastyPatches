# 随机遇敌逻辑分析

## 结论

游戏的「踩地雷」遇敌不是每走一步独立判定一次概率，而是「随机距离阈值 + 输入更新量累计」模型：进入可遇敌地图时生成一个阈值，符合条件的地图输入更新按 1 或 2 累加进度；进度达到阈值后尝试进入战斗。全局触发开关允许时，函数会在选择敌群前生成下一次阈值。

现有证据表明，`MP3001` 大地图与其他普通地图记录共用同一套阈值和累计逻辑，区别主要在于敌群 ID 的来源：

- 普通地图从记录取得查表键，再经系统表解析为敌群 ID。
- 大地图设置了特殊标志，触发时按当前位置查询 `MP3001\MP3001.MDT`，再把地块值映射为敌群 ID。

因此，遇敌率调整不需要分别改写两套概率函数。更合适的实现方式是同时处理初始阈值生成函数 `sub_403510` 和 `sub_4035A0` 中的后续阈值重抽；备选方案是在累计函数 `sub_4034C0` 中缩放增量。缩放阈值可以保留权重 1 与 2 之间的原始关系，风险更低。

## 分析基准

- 游戏文件：`E:\Games\Dynasty\Castle\exe\RPG.exe`
- 版本：2002-01-09 原版
- SHA-256：`8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`
- 工具：IDA Pro 9.4、Hex-Rays、idalib
- 主要证据：`analysis/ida_encounter_logic.txt`
- 地图记录统计：`analysis/map_encounter_records.txt`

本文中的 VA 均以镜像基址 `0x00400000` 为准。函数名是 IDA 自动生成的名称，不代表原始符号。

## 状态与数据来源

### 运行时状态

| 地址 | 暂定含义 | 写入或使用位置 |
| --- | --- | --- |
| `0x0046F608` | 已解析的当前敌群 ID | `sub_403590` 和 `0x004037C9` 写入；`0x00403800` 送入战斗状态。 |
| `0x0046F610` | 阈值随机公式的基数，固定为 150 | `0x00403524` 写入。 |
| `0x0046F614` | 下一次遇敌所需累计量 | `sub_403510` 初始化，`sub_4035A0` 每次触发后重抽。 |
| `0x0046F618` | 当前累计量 | `sub_4034C0` 增加；启用遇敌的地图初始化和触发路径会清零。禁用遇敌的地图初始化只清除阈值。 |
| `0x0046F61C` | 临时模式剩余计数上限 | 特殊流程写入 1500；平时为 0。 |
| `0x0046F620` | 临时模式已过计数 | `sub_4034C0` 每次被调用时递增。 |
| `0x0046F624` | 大地图位置查表标志 | 由地图记录 `+0x1A1` 是否非零决定。 |
| `0x0046846C` | 全局允许触发开关 | `sub_4035A0` 在正式触发前检查。 |
| `0x0046846D` | 临时模式方向：非零时阈值乘 2，零时阈值除以 3 | 特殊流程在 `0x0040CF37`、`0x0040CF6A` 设置。 |

### 地图记录

地图主记录大小为 `0x473` 字节。`sub_408E80` 加载记录时读取以下字段：

| 记录偏移 | 含义 | 证据 |
| --- | --- | --- |
| `+0x1A1` | 是否按大地图位置查询敌群 | `0x004093AA` 读取，转成 `0x0046F624`。 |
| `+0x378` | 是否启用随机遇敌 | `0x004093BB` 读取；为 0 时 `sub_403510` 清除阈值。 |
| `+0x37C` | 默认敌群查表键 | 经 `sub_44B0D0` 查系统表后写入 `0x0046F608`。原始值不是最终敌群 ID。 |

`sub_40AC00` 加载地图 SCI 时按 `0x473` 字节为步长遍历记录。对游戏目录中的 74 个 `MP*.SCI` 文件、共 96 条记录进行统计后，发现 61 条启用遇敌的记录：58 条的 `+0x378` 为 300，3 条为 250。代码只判断该字段是否为 0，没有把 250 或 300 代入概率公式；它的实际作用是布尔开关，而不是遇敌率数值。

## 计算流程

### 1. 进入地图时初始化阈值

地图记录由 `sub_408E80` 加载。关键调用点是：

```asm
004093AA  mov  al, [ebx+1A1h]
004093B5  mov  byte_46F624, dl
004093BB  mov  eax, [ebx+378h]
004093C2  call sub_403510
004093C7  mov  ecx, [ebx+37Ch]
004093CE  call sub_44B0D0
004093D4  call sub_403590
```

若 `record[0x378] == 0`，随机遇敌关闭，阈值被设为 0。否则先设置 `base = 150`，然后计算：

```c
threshold = 150 + rand() % 300;  // 150..449
progress = 0;
```

原始模式下，阈值平均约为 299.26 个累计单位。MSVC `rand()` 只产生 0 至 32767，`% 300` 有轻微取模偏差，但对功能设计没有实质影响。

### 2. 地图输入更新时累计进度

累计处理路径为：

```text
sub_409580
  -> sub_408C40
       -> sub_4034C0(1 或 2)
```

`sub_408C40` 在第一个参数非零时读取当前对象状态 `BYTE1([current+0x18])`：

- 值为 0 时累计 1。
- 值为 1 时累计 2。
- 其他值不累计。

对应逻辑为：

```c
if (threshold != 0)
    progress += movement_weight;  // 1 或 2
```

这说明计数单位不是单纯的渲染帧，也不是每次直接进行百分比判定，而是 `sub_408C40` 收到非零输入结果后的更新事件。静态证据尚不能证明被阻挡的移动是否计数，也不能给权重 2 命名；其数值和调用条件已经明确。

### 3. 达到阈值后触发

地图更新函数在 `0x0040B158` 调用 `sub_4035A0`。该函数首先处理游戏状态 `+0x118` 的其他分支；只有该状态为 0 时才进入随机遇敌判断。随机分支的前置条件为：

```c
if (transition_state != 0) return false;
if (progress < threshold) return false;
if (threshold == 0) return false;

progress = 0;
if (!encounter_globally_enabled) return false;  // 此时不重抽阈值
```

通过前置条件后，函数先为下一轮重新生成阈值，再决定本次敌群 ID：

```c
next_threshold = 150 + rand() % 300;
```

若当前地图不是大地图，继续使用加载地图时由 `sub_44B0D0(record[0x37C])` 解析出的 `current_encounter_id`。若大地图标志 `0x0046F624` 非零，则加载 `MP3001\MP3001.MDT`，按当前位置查询地块值：

```c
column = world_x / 64;
row = world_y / 48;
tile = mdt_words[4 + column + 54 * row];
current_encounter_id = system_table[4400 + tile];
```

查询结果为 0，或当前地图状态无效时，本次不进入战斗，累计量保持为 0，等待下一轮阈值。查询成功后，敌群 ID 写入全局游戏状态 `+0xD8`，随后创建战斗场景对象并切换状态。

这一分支说明 `MP3001` 大地图与其他普通地图的差异是「敌群选择方式」，不是「遇敌概率公式」。普通地图中哪些属于迷宫，可再按地图名称与实机位置确认，不影响公式结论。

## 临时阈值修正模式

游戏还有一组限时修正逻辑。事件分派在 `sub_40CBF0` 中把计数上限设为 1500，并写入模式标志。函数在 `0x0040CC5A` 已将 `EBX` 设为 1，因此 `0x0040CF6A` 写入的 `BL` 也是 1：

```asm
0040CF2D  mov dword_46F61C, 1500
0040CF37  mov byte_46846D, 0    ; 阈值除以 3

0040CF60  mov dword_46F61C, 1500
0040CF6A  mov byte_46846D, bl   ; BL = 1，阈值乘 2
```

只要 `temporary_limit > 5`，阈值会按以下方式调整：

| 模式 | 阈值范围 | 平均阈值 | 相对阈值到达频率 |
| --- | ---: | ---: | ---: |
| 原始 | 150 至 449 | 约 299.26 | 1 倍 |
| 阈值乘 2 | 300 至 898 | 约 598.52 | 约 0.5 倍 |
| 阈值除以 3 | 50 至 149 | 约 99.42 | 约 3.01 倍 |

临时计数在 `sub_4034C0` 中按更新次数递增，而不是按累计权重递增。代码使用 `temporary_elapsed > temporary_limit` 才清除模式，因此仅在计数从 0 开始的前提下，第 1501 次调用会清除模式。所见激活代码没有重置已过计数，也不会立即重抽当前阈值；模式清除后同样不会立即重抽，当前阈值会使用到下一次触发或重新初始化。

## 容易误判的地方

- `+0x378` 中常见的 250、300 不是概率分母，也不是平均步数。代码只判断它是否为 0。
- 地图记录 `+0x37C` 为 0 不等于最终敌群 ID 必然为 0；它还会经过 `sub_44B0D0` 解析。大地图则会在触发时按位置重新查询敌群 ID。
- 临时模式修改的是阈值，不是 `rand()` 成功率：标志非零时乘 2，标志为零时除以 3。
- 遇敌检查在地图更新函数中轮询，但累计量只由 `sub_408C40` 的特定非零输入更新增加。无输入站立不会仅因轮询本身增加进度；被阻挡移动是否计数仍需运行时确认。
- 一次达到阈值但敌群查询失败时，游戏已经重抽下一阈值，不会沿用旧阈值立即重试。

## 遇敌率功能的实现建议

### 推荐方案：缩放阈值

在 `sub_403510` 的初始化路径和 `sub_4035A0` 的重抽路径中，对最终阈值应用同一倍率：

```c
adjusted_threshold = max(1, round(original_threshold * 100 / encounter_rate_percent));
```

例如：

| 设置 | 阈值倍率 | 预期阈值到达频率 |
| --- | ---: | ---: |
| 50% | 2.0 | 原始的一半 |
| 100% | 1.0 | 原始值 |
| 200% | 0.5 | 原始的约 2 倍 |
| 0% | 关闭阈值或跳过触发 | 不发生随机遇敌 |

推荐缩放「游戏内置模式处理完后的最终阈值」，这样内置的乘 2、除以 3 仍然保留，并与自定义倍率相乘。例如 200% 设置叠加内置的阈值乘 2 后，阈值到达频率约回到原始值。

这些百分比描述的是固定输入权重分布下的「阈值到达频率」，不是成功进入战斗的精确频率。整数取整、越过阈值后丢弃超额累计量、敌群解析结果为 0、地图状态无效和全局开关关闭都会造成差异。

两个阈值生成点必须同时处理：

- `sub_403510`：进入地图或重新加载地图时生成初始阈值。
- `sub_4035A0`：每次达到阈值后生成下一阈值。

只修改其中一处会导致第一次遇敌与后续遇敌采用不同设置。

### 备选方案：缩放累计量

也可以在 `sub_4034C0` 中缩放 `movement_weight`。这种方式只需钩住一个累计入口，但会改变原有权重 1 与 2 的离散行为，而且小于 100% 时需要保存小数余量，否则整数截断会造成明显偏差。除非需要动态、连续地改变当前剩余距离，否则不建议采用。

### 0% 模式

0% 不应把除数设为 0 后继续走普通公式。较安全的行为是：

1. 地图加载时保留原始敌群数据，只令随机遇敌阈值为 0。
2. 从 0% 恢复为非零设置时，立即按当前倍率生成新阈值并清零累计量。
3. 不在 `sub_4035A0` 函数入口直接返回，因为它在随机分支前还处理游戏状态 `+0x118`；只让随机遇敌阈值保持为 0。
4. 不修改剧情战、固定事件战或战斗场景构建函数。

随机阈值分支集中在 `sub_4035A0`，而剧情和脚本战斗还有其他状态切换路径。补丁只处理该函数中的随机分支和阈值生成，避免误伤固定战斗。

## 推荐补丁边界

若后续实现运行时 DLL 钩子，建议把倍率计算放在注入模块内，并保留以下约束：

- 只修改新建且仍处于挂起状态的目标进程。
- 校验原始字节后再写入跳板。
- 覆盖完整指令边界，返回地址分别落在原函数的合法后继指令。
- 使用整数定点计算，避免改变目标进程的浮点状态。
- 对结果设置下限 1，并防止高倍率或配置损坏导致 32 位溢出。
- 不改 `MP*.SCI`、`MP3001.MDT` 或原始 `RPG.exe` 文件。

候选拦截位置有两类：

1. 对 `sub_403510` 使用完整函数 detour，或分别覆盖其普通、乘 2 和除以 3 三条终止路径；该函数没有覆盖所有模式的单一公共后继点。
2. 在 `sub_4035A0` 中可于三条阈值路径汇合后的 `0x00403761` 附近统一处理。
3. 抽出一个共享的远程函数，让两个入口使用同一倍率公式，避免实现产生差异。

若功能只在启动前配置，已生成阈值的动态重算问题不存在。若未来允许游戏运行中修改设置，需要明确是保留当前剩余距离、按比例重算，还是清零累计量并重抽；三种行为的体验不同。

实现前还应做一次运行时验证：在普通地图、`MP3001` 大地图、阈值乘 2 和阈值除以 3 状态下记录 `threshold`、`progress`、输入结果、权重与敌群 ID，确认被阻挡移动、权重 2 的实际含义和敌群解析失败条件。

## 证据复现

将 IDA 数据库复制到临时目录以避免残留的解包数据库文件影响 idalib，然后执行：

```powershell
python tools/ida_encounter_analysis.py `
  "C:\Users\Soar\AppData\Local\Temp\opencode\Castle-RPG.i64" `
  "analysis\ida_encounter_logic.txt"

python tools/audit_encounter_map_records.py `
  "E:\Games\Dynasty\Castle\multimedia\Map" `
  "analysis\map_encounter_records.txt"
```

`tools/ida_encounter_analysis.py` 导出核心函数、调用点和伪代码；`tools/audit_encounter_map_records.py` 统计地图记录中的启用值、特殊标志和敌群查表键。两个脚本不会启动游戏或修改游戏输入文件，但会覆盖指定的分析报告。
