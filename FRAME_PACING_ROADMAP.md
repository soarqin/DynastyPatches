# 帧同步方案实施路线图

## 目标边界

目标是：逻辑继续保持原版约 20 Hz，消除画面提交过程中的撕裂，同时不改变移动速度、动画速度、输入节奏、计时器和事件处理。

“把逻辑循环提高到 60 Hz”不属于候选方案；“重复提交同一张画面”也不等于提高有效帧率，只能用于诊断异常呈现节拍。

每个阶段都必须独立构建、启动和人工测试。只有前一阶段确认不能解决问题，或明确存在不可接受的副作用，才进入下一阶段。

## 优先级总表

| 优先级 | 方案 | 预期收益 | 主要风险 | 推进判断 |
| --- | --- | --- | --- | --- |
| P0 | 只做运行时测量 | 确认撕裂位置、逻辑周期和 `Blt` 时序 | 几乎无行为风险；需要可观测性 | 必做，作为所有方案的基线 |
| P1 | 保持窗口化，让 DWM/桌面合成负责同步 | 改动最少；可能天然避免窗口撕裂 | 不一定有效；不同系统设置行为不同 | 若已无可复现撕裂则停止 |
| P2 | 同线程 `WaitForVerticalBlank` 实验 | 实现简单；可能直接消除 DirectDraw 提交撕裂 | 阻塞逻辑线程，造成轻微或明显掉速 | 只作为短期实验，不满意即回退 |
| P3 | 快照交换 + 独立呈现线程 | 呈现等待不再阻塞逻辑；最符合目标 | 跨线程生命周期、surface 丢失、退出同步和快照复制成本 | P2 确认有效但拖慢逻辑时进入 |
| P4 | 直接把原 DirectDraw surface 的 `Blt` 移到线程 | 理论上无需额外快照 | 共享 surface 竞态、撕裂、`SURFACEBUSY`、悬空访问 | 不建议实现，除非逆向证明对象可安全跨线程 |
| P5 | 逻辑 20 Hz 下的视觉插值/独立动画 | 可能改善运动顿挫，不只是消除撕裂 | 需要拆状态、识别动画字段，风险和工作量最大 | 本需求后续独立项目 |

## P0：建立基线

### 当前进度

第一阶段已经完成：

- 静态确认窗口 `Blt` 使用 `DDBLT_WAIT (0x01000000)`，它只等待 bitblt 可以提交，不是垂直同步；
- 静态确认全屏 `Flip` 使用 `DDFLIP_DONOTWAIT (1)`；
- 增加默认关闭的呈现诊断钩子，统计 `sub_4064E0` 的调用间隔和执行耗时；
- 诊断启动时输出 DWM 状态、桌面分辨率、刷新率和色深。

启用方式是在启动器旁的 `CastlePatches.ini` 中加入：

```ini
[Display]
FramePacingDiagnostics=1
```

当前诊断依赖运行时 DLL 注入，人工测试时同时启用窗口模式。使用 DebugView 或调试器捕获 `CastleRuntime:` 开头的 `OutputDebugStringW`。每 100 次呈现输出一组统计；关闭该配置后不安装诊断钩子。

### 实现范围

先完成静态检查，再增加诊断，不改变游戏行为。静态检查包括：

- 确认全屏 `Flip` 的 flags `1` 为 `DDFLIP_DONOTWAIT`，并继续搜索是否还有 `DDFLIP_NOVSYNC` 路径；
- 按 DirectDraw SDK 定义重新标注窗口 `Blt` 的 flags，不能把 surface 等待误判为垂直同步；
- 搜索 `WaitForVerticalBlank`、`DwmFlush` 和其他呈现同步调用的 IAT 引用。

运行时只记录：

- 逻辑/绘制入口 `sub_405A10` 的调用间隔；
- `sub_406330` 和 `sub_4064E0` 的调用间隔与耗时；
- 窗口模式 `Blt` 的返回值和失败码；
- DirectDraw surface 恢复次数；
- 窗口是否由 DWM 合成，以及当前显示器刷新率。

诊断输出应进入调试输出或独立日志，不能使用会阻塞游戏线程的可见弹窗。

### 人工测试

在固定场景下测试静态界面、地图横向滚动、快速镜头移动、窗口拖动和切换全屏/窗口。记录是否能稳定复现撕裂，以及开启诊断后逻辑速度是否变化。

### 通过条件

得到可重复的基线：逻辑约 20 Hz，明确是否存在 `Blt` 跨扫描线现象，并能区分撕裂和单纯的 20 Hz 运动顿挫。由于 VRR 会改变扫描时序，不能直接用当前显示器的肉眼观感作为通过条件。

### 实测结论

当前实测数据显示：稳定运行时呈现间隔约 `50 ms`，呈现函数平均耗时约 `0.20` 至 `0.32 ms`，最大值通常低于 `1 ms`。这确认游戏确实约 20 Hz 呈现，并且现有 `DDBLT_WAIT` 没有阻塞到显示器垂直消隐。少量 `4` 至 `11 ms` 的短间隔和约 `100` 至 `115 ms` 的长间隔属于主循环/线程调度抖动，不能视为 60 Hz 呈现。

P0 的静态与 CPU 侧测量已经完成。显示链路验证仍未完成，因为 CPU 侧调用耗时本身无法观测扫描线位置。人工复现前应关闭显示器 VRR、Windows 动态刷新率和驱动自适应同步，固定显示器为 60 Hz；仍无法确认时使用高帧率屏幕录制、采集卡或显示器 OSD 逐帧检查。

如果无法复现撕裂，停止实现后续同步补丁；只保留诊断或移除诊断代码。

## P1：利用窗口合成

### 当前状态

P1 必须拆成两条显示路径，窗口化不是本补丁的前提：

- **P1-F：全屏独占 DirectDraw**。确认 `DDSCL_FULLSCREEN | DDSCL_EXCLUSIVE`、显示模式和 `Flip` 的实际行为；重点验证 `DDFLIP_DONOTWAIT` 在当前驱动/兼容层下是否仍由硬件在垂直消隐执行物理翻转。
- **P1-W：窗口化 DWM 合成**。只作为窗口模式的独立兼容路径验证，不代表全屏方案已经解决。

静态检查确认现有窗口模式已经符合 P1-W 的最低条件：

- DirectDraw 使用 `DDSCL_NORMAL` 窗口化协作级别；
- 窗口样式为 `WS_CAPTION | WS_SYSMENU | WS_MINIMIZEBOX`，没有发现 `WS_EX_LAYERED` 或 `WS_EX_NOREDIRECTIONBITMAP`；
- 呈现目标是窗口客户区，最终由 Windows 窗口合成链处理；
- 没有新增等待、异步 `Blt` 或独立呈现线程。

全屏静态路径也已确认：原程序在全屏分支设置独占协作级别，并由 `sub_4064E0` 调用 `Flip`；当前没有 `WaitForVerticalBlank` 调用。P1 当前不需要新增同步代码，它是对两条现有呈现路径的受控人工验证。P0 诊断现在在 `[Display] FramePacingDiagnostics=1` 时也会注入全屏目标进程，可记录全屏 `Flip` 的调用间隔和耗时。

### 实现范围

继续使用现有窗口化 DirectDraw 路径，不新增线程、不等待垂直消隐。确认窗口样式、DWM 合成和显示缩放组合下的实际表现；必要时只修正窗口模式下的呈现配置。

### 收益与限制

这是最低风险路径。现代 Windows 通常由 DWM 负责窗口最终合成，可能已经避免直接扫描输出撕裂。但它无法保证旧 DirectDraw 兼容路径在所有系统、显卡驱动和 DWM 设置下都一致。

### 通过条件

P1-F 的通过条件是在固定刷新率、关闭 VRR/DRR 的环境下，独占全屏地图滚动和快速镜头移动没有可确认的撕裂，且 P0 的逻辑周期、输入响应和计时行为不变。若满足，保留原始全屏 `Flip` 路径，不增加同步代码。

P1-W 的通过条件是在目标系统和至少一个备用系统上，窗口地图滚动及窗口移动均无可确认的撕裂，且逻辑行为不变。它只决定窗口路径是否需要继续推进，不影响 P1-F 的结论。

静态结论：窗口路径目前没有补丁级 VSync，只是依赖 DWM 合成。全屏 `FullscreenVSync` 跳板不覆盖窗口分支；窗口 `DDBLT_WAIT` 也不能替代 VSync。因此即使全屏 P2-F2 已作为阶段成果固定，窗口路径仍需单独完成 P1-W 人工验证。

测试时固定 60 Hz，并关闭 VRR、Windows 动态刷新率和驱动自适应同步。P1-F 至少覆盖独占全屏地图滚动、快速镜头移动、切换显示模式和退出；P1-W 另外覆盖窗口拖动、最小化恢复和场景切换。建议使用高帧率录屏或采集卡逐帧确认，而不是只依赖 VRR 环境下的肉眼观察。

如果任一路径在受控固定刷新率环境下仍能确认撕裂，该路径的 P1 判定失败，下一步才对该路径进入 P2；如果无法确认撕裂，则保留该路径，不增加同步补丁。

若窗口路径确认仍有撕裂，下一阶段应命名为 P2-W，单独评估 `DwmFlush` 或独立呈现线程；不能直接复用全屏 `WaitForVerticalBlank` 跳板，因为窗口模式使用的是 `Blt` 和 DWM 合成链。

### 全屏实测结论

全屏诊断环境为 DWM 开启、`3440x1440`、`60 Hz`、`32 bpp`。稳定窗口的呈现间隔约 `50 ms`，呈现函数平均耗时约 `0.20` 至 `0.42 ms`，偶发最大值约 `3` 至 `6.4 ms`，没有稳定的 `16.7 ms` 垂直消隐等待。P1-F 因此确认当前全屏 `DDFLIP_DONOTWAIT` 路径没有阻塞式同步，但尚不能仅凭日志断定屏幕必然撕裂。

全屏路径若需要继续验证，应进入 P2-F：只将 `Flip` 的非等待参数改为默认等待语义，窗口路径不变，并单独比较逻辑间隔、输入延迟和撕裂表现。

## P2：同线程垂直消隐实验

### P2-F 当前实验实现

全屏路径已加入默认关闭的最小实验补丁。配置：

```ini
[Display]
FullscreenVSync=1
```

该补丁在 `0x004064ED` 建立跳板，在全屏 `Flip` 前调用 DirectDraw 接口的 `WaitForVerticalBlank(DDWAITVB_BLOCKBEGIN, NULL)`，然后恢复原有的 `mov esi,[esi+4]; push 1` 和 `Flip` 调用；窗口模式不会应用此补丁。它不是新的线程架构，也不修改逻辑帧率，目的是测量显式 VBlank 等待是否消除撕裂以及会给逻辑线程增加多少延迟。

### P2-F 实测结论

开启补丁后，稳定窗口仍约 `50 ms`，呈现平均耗时约 `0.23` 至 `0.42 ms`，没有稳定的 `16.7 ms` 阻塞；偶发的 `3` 至 `9.6 ms` 峰值属于非稳定抖动。因此 `push 1` 改为 `push 0` 没有提供可测的 VSync 效果，P2-F 判定为无效。下一步应评估显式 `WaitForVerticalBlank`（P2-F2），或在确认不接受同线程等待后进入 P3 的独立呈现方案。

### 实现范围

在窗口模式 `sub_4064E0` 的 `Blt` 前调用 DirectDraw 的垂直消隐等待接口。必须保留原始路径和字节校验，实验补丁可配置关闭。

等待只能位于呈现阶段，不能插入逻辑更新入口。记录每次等待耗时，并设置异常超时或失败回退，避免显示驱动异常时永久阻塞游戏。

### 收益与风险

实现范围小，最有可能快速验证 DirectDraw 是否能消除当前撕裂。代价是等待发生在逻辑线程，可能让下一次逻辑更新延迟最多接近一个显示周期；窗口化模式下接口也可能不生效。

### 通过条件

- 撕裂明显减少或消失；
- 逻辑周期没有出现可感知变慢、输入延迟或计时异常；
- 场景切换、视频播放、最小化恢复和退出均稳定。

任一条件不满足都回退到 P1，并进入 P3，不继续扩大同线程等待逻辑。

## P3：快照交换与独立呈现线程

### 实现范围

这是首选的正式候选方案：

1. 逻辑线程继续调用原绘制路径，完成一张完整 640 × 480 画面。
2. 将后备画面复制到独立的 16 位快照缓冲区，或完成两个独立系统内存 surface 之间的交换。
3. 逻辑线程只通过序号或指针发布已完成缓冲区，不等待呈现线程。
4. 呈现线程读取最近一张完整快照，在适当时机等待垂直消隐并执行 `Blt`。
5. 场景切换、surface 丢失和窗口销毁时停止发布并回收呈现资源。

逻辑线程不能把正在绘制的游戏 surface 直接交给呈现线程；呈现线程只能访问已经发布的完整快照。

### 收益与风险

它把垂直同步等待从逻辑线程移出，最符合“不改变逻辑速度、消除撕裂”的目标。新增成本包括 640 × 480 × 2 字节快照复制、线程同步、DirectDraw/DWM 兼容性和退出时序。

### 通过条件

- P0 统计显示逻辑调用间隔与未加同步时一致；
- 地图滚动无撕裂；
- 快照不会出现半帧、花屏或旧新画面混合；
- 最小化、恢复、切换场景、播放 Bink 视频和退出连续测试稳定；
- CPU 占用和输入延迟在可接受范围内。

如果线程方案只能减少撕裂但引入画面延迟，应记录延迟量，并优先尝试双缓冲与三缓冲策略，而不是把逻辑线程重新阻塞。

## P4：直接跨线程使用原始 surface

该方案只作为逆向验证对象，不作为默认实现。只有在确认 DirectDraw 对象的所有调用、锁定、恢复和释放路径都受同一同步协议保护时才可能考虑。否则它比 P3 少一次复制，却把风险扩大到整个渲染资源生命周期，收益不值得。

## P5：视觉插值与独立动画

该方案解决的是 20 Hz 的运动顿挫，不是撕裂。它需要为角色、镜头、特效等视觉字段保存前后逻辑状态，并确认这些字段不会参与碰撞、脚本、随机数或存档。应另立需求，不能作为本次撕裂修复的隐式扩展。

## 统一回退规则

- 每个实验都必须有独立开关，默认关闭新行为，直到人工测试通过。
- 任何 patch site、DirectDraw 接口或快照资源初始化失败，都回退到原始呈现路径。
- 不在测试中修改磁盘上的原始 `RPG.exe`，只对新建的挂起进程注入。
- 发现崩溃、逻辑变速或状态错乱时，保留诊断信息并回到上一阶段，不用静默回退掩盖原因。

## 推荐顺序

```text
P0 测量
  -> 无法复现撕裂：停止
  -> P1 窗口合成已解决：停止
  -> P2 同线程垂直同步有效且无副作用：停止
  -> P2 有效但拖慢逻辑：P3 快照 + 呈现线程
  -> P3 仍需改善运动连续性：另立 P5 视觉插值项目
```
