# 动态遇敌率功能实施计划

## 目标

为《幽城幻剑录》增加运行时动态遇敌率调整功能。玩家可在游戏运行期间通过组合键切换遇敌率，无需退出游戏或重新启动补丁工具。

功能只调整随机遇敌的阈值到达频率，不修改剧情战、固定事件战、敌群配置或游戏数据文件。

## 第一版范围

### 遇敌率档位

按以下顺序循环：

```text
0% -> 50% -> 75% -> 100% -> 150% -> 200% -> 300%
```

其中：

- `0%`：关闭随机遇敌。
- `100%`：保持原始游戏频率。
- 小于 `100%`：降低阈值到达频率。
- 大于 `100%`：提高阈值到达频率。

百分比描述的是「遇敌阈值到达频率」，不保证成功进入战斗的次数严格按相同比例变化。敌群解析结果、地图状态和整数取整仍会影响最终战斗次数。

### 默认按键

| 按键 | 行为 |
| --- | --- |
| `Ctrl+F11` | 降低一档 |
| `Ctrl+F12` | 提高一档 |
| `Ctrl+F10` | 恢复 `100%` |

按键仅在游戏窗口接收输入时生效。忽略键盘自动重复，按住组合键不会连续切换。

### 启动器选项

启动器增加「动态调整遇敌率」复选框，并将选择写入：

```ini
[Patches]
DynamicEncounterRate=1
```

第一版固定以 `100%` 启动。游戏中的按键调整只对当前进程有效，不回写 INI。

## 技术方案

### 运行位置

功能放在注入目标进程的 `CastleRuntime.dll` 内。启动器只负责：

1. 保存是否启用动态遇敌率。
2. 创建挂起的游戏进程。
3. 注入运行时 DLL。
4. 完成补丁安装后恢复游戏线程。

启动器退出后，按键处理和阈值调整继续在游戏进程内运行。

### 运行时状态

DLL 增加以下状态：

```c
static volatile LONG g_encounter_rate_percent = 100;
static bool g_encounter_map_enabled;
static WNDPROC g_original_window_proc;
```

含义如下：

| 状态 | 含义 |
| --- | --- |
| `g_encounter_rate_percent` | 当前档位，取值为 `0/50/75/100/150/200/300`。 |
| `g_encounter_map_enabled` | 当前地图记录 `+0x378` 是否允许随机遇敌。 |
| `g_original_window_proc` | 游戏原始窗口过程，用于转发未处理的窗口消息。 |

阈值跳板只读取 DLL 可写数据，不把动态状态放入可执行内存。补丁代码分配为可执行只读，动态变量保留在 DLL 的可写数据段。

## 按键处理

### 窗口过程子类化

运行时工作线程取得有效游戏窗口句柄后，使用 `SetWindowLongPtrW` 安装窗口过程：

```text
游戏窗口过程
  -> RuntimeWindowProcedure
       -> 识别 Ctrl+F10/F11/F12
       -> 忽略自动重复
       -> 更新遇敌率
       -> 重算当前阈值和累计量
       -> 吞掉已处理消息
  -> CallWindowProcW 转发其他消息
```

仅处理 `WM_KEYDOWN` 或 `WM_SYSKEYDOWN` 的首次按下事件。若 `lParam` 第 30 位为 1，表示自动重复，不执行切换。

窗口销毁或 DLL 停止使用窗口时，不再访问窗口句柄。第一版不卸载 DLL，因此不需要在游戏运行期间恢复窗口过程；仍需保证窗口销毁后的工作线程能够正常退出。

### 降级方案

若实测全屏模式不稳定地产生键盘窗口消息，则改为运行时工作线程轮询 `GetAsyncKeyState`，并同时满足以下条件才处理：

- `GetForegroundWindow() == g_window`。
- 组合键从未按下变为按下。
- 当前不是自动连续触发状态。

优先实现窗口过程方案，轮询只作为实测失败后的替代方案。

## 阈值调整

### 原始公式

游戏生成的原始阈值为：

```c
threshold = 150 + rand() % 300;
```

游戏内置临时模式可能继续对结果乘 2 或除以 3。自定义遇敌率应在这些内置修正完成后应用。

### 自定义公式

```c
adjusted_threshold = ceil(original_threshold * 100 / rate_percent);
```

使用无符号 64 位中间值实现：

```c
adjusted_threshold =
    (uint32_t)(((uint64_t)original_threshold * 100u + rate_percent - 1u) /
               rate_percent);
if (adjusted_threshold == 0) adjusted_threshold = 1;
```

`rate_percent == 0` 单独处理，不进入除法。

### 初始阈值补丁

地图记录加载路径为：

```text
sub_408E80
  -> sub_403510(record[0x378])
```

候选拦截点为 `0x004093C7`，即 `sub_403510` 返回后。此时：

- 原始阈值已经生成。
- 游戏内置阈值修正已经完成。
- `EBX` 仍指向当前地图记录。
- 可以用 `record[0x378] != 0` 更新 `g_encounter_map_enabled`。
- 可以统一缩放 `0x0046F614` 中的最终阈值。

实施前已用 IDA 核对该地址开始的完整指令边界、覆盖长度和原始机器码。详细证据见 `analysis/encounter_patch_sites.txt`。运行时仍会在目标进程中再次校验原始字节，只有完全匹配时才安装跳板。

### 后续阈值补丁

每次阈值到达后，`sub_4035A0` 会重新生成下一次阈值。普通、乘 2 和除以 3 三条路径在 `0x00403761` 附近汇合。

候选拦截点为 `0x00403761`。跳板应在敌群选择开始前：

1. 保留原函数需要的寄存器和标志。
2. 读取 `0x0046F614`。
3. 按当前档位缩放阈值。
4. 写回 `0x0046F614`。
5. 重放被覆盖指令并返回原函数。

实施前已核对该位置的完整指令边界和分支目标，确认没有分支落入被覆盖指令中部。详细证据见 `analysis/encounter_patch_sites.txt`。

## 动态切换语义

### 非零档位切换

切换时同步缩放当前阈值与累计量，近似保持已经完成的行程比例。

设：

- `T`：当前阈值 `0x0046F614`。
- `P`：当前累计量 `0x0046F618`。
- `Rold`：切换前档位。
- `Rnew`：切换后档位。

计算：

```c
new_threshold = ceil(T * Rold / Rnew);
new_progress = floor(P * Rold / Rnew);
```

边界处理：

- `T == 0` 时不生成阈值，只更新档位。
- `new_threshold` 下限为 1。
- `new_progress` 不得大于 `new_threshold - 1`，防止按键后立即无条件触发。
- 使用 64 位中间值避免乘法溢出。

所有游戏遇敌全局变量的修改都在游戏窗口线程执行，不从后台线程并发写入。

### 切换到 0%

```c
g_encounter_rate_percent = 0;
*(uint32_t *)0x0046F614 = 0;
*(uint32_t *)0x0046F618 = 0;
```

不在 `sub_4035A0` 入口直接返回。该函数在随机遇敌分支前还处理游戏状态 `+0x118`，整体跳过会影响其他游戏流程。

### 从 0% 恢复

若 `g_encounter_map_enabled == false`，保持阈值和累计量为 0。

若当前地图允许随机遇敌：

1. 在游戏窗口线程调用原始 `sub_403510(1)`。
2. 让游戏按当前内置临时模式生成阈值。
3. 按恢复后的档位缩放新阈值。
4. 保持累计量为 0。

恢复时重新抽取阈值，不使用关闭前保存的旧阈值。这样可以反映 0% 期间已经发生变化的游戏内置临时模式。

## 状态反馈

### 第一版

- 窗口模式：临时将窗口标题改为「遇敌率：150%」，约 1.5 秒后恢复原标题。
- 全屏模式：调用 `MessageBeep` 给出按键已接受提示。
- `Ctrl+F10` 始终恢复 `100%`，作为不可见状态下的确定恢复操作。
- 使用 `OutputDebugStringW` 记录档位、旧阈值、新阈值和累计量，供调试版本验证。

标题恢复不能通过阻塞窗口线程等待。工作线程记录恢复时间，并在延迟结束后通过窗口消息请求恢复标题。

### 后续版本

若需要在全屏下显示精确百分比，可单独增加游戏画面 OSD。该功能需要接入呈现或文本渲染路径，不纳入第一版，避免扩大初始补丁范围。

## 启动流程调整

当前运行时 DLL 只在窗口模式、经验倍率或金钱倍率启用时注入。条件调整为：

```c
if (options.windowed ||
    options.experience_multiplier != 1 ||
    options.money_multiplier != 1 ||
    options.dynamic_encounter_rate) {
    InjectRuntimeDll(process);
}
```

`CastleRuntimeStart` 当前在非窗口模式下会提前返回。调整后的顺序为：

```text
读取配置
安装经验和金钱补丁
动态遇敌率启用时安装两个遇敌阈值跳板
需要窗口或按键功能时创建 RuntimeWorker
RuntimeWorker 等待游戏窗口
动态遇敌率启用时安装窗口过程
窗口模式启用时调整窗口并安装鼠标钩子
```

这样全屏模式也能使用动态遇敌率。

## 失败处理

### 启动前失败

以下任一情况发生时，`CastleRuntimeStart` 返回独立错误码，启动器终止仍处于挂起状态的目标进程：

- 初始阈值补丁点原始字节不匹配。
- 后续阈值补丁点原始字节不匹配。
- 跳板内存分配失败。
- 代码页保护或写入失败。
- 跳转位移不可表示。

不得在补丁部分安装后继续启动游戏。

### 运行时失败

- 无法子类化窗口时，记录诊断并禁用按键调整；不影响原始游戏运行。
- 标题反馈失败时不回滚档位调整。
- 当前阈值或累计量出现异常值时，恢复为安全状态：阈值重新生成、累计量清零，并记录诊断。

## 实施步骤

### 阶段 1：补丁位点确认

1. 用 IDA 导出 `0x004093C7` 和 `0x00403761` 周围的完整基本块。
2. 确认覆盖长度、原始机器码、进入边和返回地址。
3. 确认调用 `sub_403510` 时的调用约定和可安全调用条件。
4. 将证据保存到 `analysis/`。

完成标准：两个跳板均有明确的原始字节、覆盖指令和返回地址。

### 阶段 2：启动器配置

1. 扩展 `LaunchOptions`，增加 `dynamic_encounter_rate`。
2. 增加复选框和 INI 读写。
3. 修改运行时 DLL 注入条件。
4. 保持现有窗口布局可读，不引入与功能无关的界面调整。

完成标准：启用该选项时，全屏和窗口模式都会注入运行时 DLL。

### 阶段 3：运行时阈值补丁

1. 增加档位状态和阈值缩放函数。
2. 安装初始阈值跳板。
3. 安装后续阈值跳板。
4. 保留游戏内置乘 2、除以 3 修正。
5. 为补丁安装分配独立错误码。

完成标准：固定在 `100%` 时，阈值行为与原版一致；其他固定档位按公式缩放。

### 阶段 4：按键与动态重算

1. 子类化游戏窗口过程。
2. 实现组合键和自动重复过滤。
3. 实现非零档位切换的比例重算。
4. 实现 0% 的关闭和恢复行为。
5. 增加标题、提示音和调试日志反馈。

完成标准：游戏运行期间可反复切换档位，无需启动器保持运行。

### 阶段 5：验证与文档

1. 构建 Win32 运行时 DLL。
2. 构建 Win32 和 x64 启动器。
3. 执行静态字节校验和跳板边界复核。
4. 在测试存档中手动验证普通地图和 `MP3001` 大地图。
5. 更新 `ANALYSIS.md`、README 和 CastlePatches 变更记录。

完成标准：构建通过，手动验证矩阵全部完成，且原始游戏文件未被修改。

## 验证矩阵

| 项目 | 验证内容 |
| --- | --- |
| 原始档位 | `100%` 下初始与后续阈值保持原始范围。 |
| 降低档位 | `50%`、`75%` 下阈值按公式增大。 |
| 提高档位 | `150%`、`200%`、`300%` 下阈值按公式减小且不低于 1。 |
| 关闭遇敌 | `0%` 下普通地图和大地图不发生随机战。 |
| 恢复遇敌 | 从 `0%` 恢复后重新抽取阈值并正常触发。 |
| 动态比例 | 非零档位间切换后，当前完成比例基本保持。 |
| 普通地图 | 使用地图记录查表键解析敌群，不进入大地图位置查询。 |
| 大地图 | `MP3001` 继续按位置查询敌群，只缩放触发阈值。 |
| 内置修正 | 自定义倍率与游戏内置乘 2、除以 3 正确叠加。 |
| 输入边界 | 自动重复、快速交替按键和失去焦点不会连续误触发。 |
| 固定战斗 | 剧情战和固定事件战不受影响。 |
| 显示模式 | 全屏与窗口模式均可调整。 |
| 启动器退出 | 启动器关闭后按键功能继续有效。 |
| 版本校验 | 原始字节不匹配时拒绝启动，不写入部分补丁。 |

## 实施前待确认

以下项目不阻塞方案可行性；其中前两项已在编码前完成，后三项仍需实机验证：

1. 已确认 `0x004093C7` 的最终覆盖指令和原始字节。
2. 已确认 `0x00403761` 的最终覆盖指令、所有进入边和原始字节。
3. 全屏模式下游戏窗口是否稳定接收 `WM_KEYDOWN`。
4. 被阻挡移动是否会让 `sub_408C40` 收到非零输入结果。
5. 权重 2 对应的实际移动状态。

前两项决定补丁安全性；后三项决定运行时验证口径和是否启用按键轮询降级方案。
