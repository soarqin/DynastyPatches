# 逆向与补丁分析

## 基准和方法

比对基准为 `E:\Games\Dynasty\Castle\exe\RPG.exe` 的 2002-01-09 原版，SHA-256 为 `8294839343b1a7845ddae31ed16216b05850efd39a742e5ca7701aadca97287f`。3 个补丁文件与基准的差异分别为：

| 文件 | SHA-256 | 变化字节数 |
| --- | --- | ---: |
| `2in1_nocd\RPG.exe` | `b10c65f56051e5a625b6c34857bcb73bd002efe3c158b6bd0cc2bb17fa871dcf` | 10 |
| `3in1_nocd\RPG.exe` | `7a933ae498fc41b7ef09bf7ad1b581b3573dc1133dbb04c4210137642b69dbef` | 15 |
| `5in1_nocd\RPG.exe` | `0c214d911382c5b652c1b89c8fae779f0f2e1f9506aba6c8b06be21e337d9aac` | 33 |

所有变化位于 `.text` 段，其文件偏移与 RVA 相同，装载基址为 `0x00400000`。功能归属由补丁版本的增量关系、`readme.txt` 和 IDA Pro 9.4 的控制流/调用点共同确认。

## 各补丁位点

| 功能 | 原始 VA | 原始机器码 | 补丁机器码 | 逆向结论 |
| --- | --- | --- | --- | --- |
| 免 CD | `0x00402A3F` | `83 F8 05 75` | `83 F8 02 7C` | `GetDriveTypeA` 的条件由「必须为 `DRIVE_CDROM`」改成「类型不得低于 `DRIVE_REMOVABLE`」。 |
| 免 CD | `0x00402A62` | `74 2E` | `EB 2E` | `MultiMedia\Mov` 的 `access` 结果不再阻断流程，直接采用当前盘符。 |
| 修复「冥狱杀阵」 | `0x00423590` | `83 FE 28` | `83 FE 30` | `sub_423550` 以 8 字节为步长处理正值条目，循环上界从 `0x28` 扩展到 `0x30`，即从 5 组扩展至 6 组。调用方会将这些条目复制回角色数据；第 6 组此前被遗漏。 |
| 修复抗性 | `0x00440E90` 等 6 处 | 目标字段为 `+0x74` 至 `+0x88` | 全部左移 4 字节 | `sub_440C60` 从菜单数据复制 6 个抗性字段。补丁把目标偏移改为 `+0x70` 至 `+0x84`，与角色当前结构对齐。 |
| 随时存档 | `0x0040A0C2` | `8B 80 80 03 00 00` | `B8 01 00 00 00 90` | 一个虚表字段读取被替换为 `return 1`。它的唯一调用方在状态切换时用返回值参与存档/场景对象创建条件，因此该条件恒为可用。 |
| 最大掉宝 | `0x00443A22` | `99 B9 64 00 00 00 F7 F9` | `99 BA 01 00 00 00 90 90` | 原逻辑计算 `rand() % 100`，并用余数与掉落阈值比较。补丁直接令余数为 1，使有效阈值必然通过。 |
| 最大成长 | `0x00443BB9`、`0x00443BFB` | `99 B9 03 00 00 00 F7 F9` | `99 BA 02 00 00 00 90 90` | 两次 `rand() % 3` 均被固定为最大余数 2。 |
| 最大成长 | `0x00443D7D` | `F7 FE 8B C2 03 C7` | `8D 44 3E FF 90 90` | 原逻辑返回区间 `[edi, edi + esi - 1]` 内的随机数；补丁直接返回上界 `edi + esi - 1`。 |

`2in1_nocd` 包含免 CD 和两个缺陷修复；`3in1_nocd` 在此基础上增加随时存档；`5in1_nocd` 再增加最大成长和最大掉宝。README 虽将 `2in1` 的项目编号写为 1、2、3，但可执行文件的增量关系与上述分组一致。

## 战斗结算倍率

IDA Pro 9.4 对原始 `RPG.exe` 的分析表明，战斗结算主路径为 `sub_443660`。该函数先累计战斗队伍的经验总值，随后逐个角色结算成长、掉落和界面提示。

| 功能 | 结算位点 | 原始机器码 | 逆向结论 |
| --- | --- | --- | --- |
| 经验倍率 | `0x00443856`、`0x004437F7` | `03 C3 89 47 24`、`8B 0D B0 B2 46 00 53` | 前者以本场累计经验 `EBX` 累加已在 `EAX` 中的角色经验并写回 `[EDI+24h]`；运行时跳板仅覆盖该完整的 5 字节序列。后者将同一数值作为「获得经验值」文本的 `sprintf` 参数。两个精确位点均通过运行时跳板使用同一倍率。 |
| 金钱倍率 | `0x00439824`、`0x004439AF` | `8B 91 D8 5D 00 00 03 D0`、`8B 08 8B 50 08 51 55 52 8D 84 24 B4 00 00 00` | 前者在 `sub_439820` 将 `EAX` 金钱增量累加到游戏状态；后者取得文本颜色参数后将本场金钱 `EBP` 压入「获得金钱」文本的 `sprintf` 参数栈。跳板从完整指令边界覆盖并使用同一倍率。 |

结算文本采用 Big5 编码：`0x0046C540` 为「獲得金錢」，`0x0046C558` 为「獲得經驗值」。两处分别在 `0x004439BE`、`0x00443809` 被 `sub_443660` 引用，确认上述字段更新属于同一战斗结算路径。

启动器将倍率限制为 1 至 10，并写入 `CastlePatches.ini` 的 `[Patches]` 段。仅当任一倍率大于 1 时才注入运行时 DLL；补丁只作用于新建的目标进程内存。

## 窗口模式

### 已确认的渲染路径

默认窗口由 `sub_401820` 在 `0x00401853` 创建：

```asm
push 1E0h          ; height = 480
push 280h          ; width  = 640
push 80000000h     ; WS_POPUP
call CreateWindowExA
```

`sub_405BD0` 是 DirectDraw 初始化。对象偏移 `+0x30` 为真时，代码进入全屏分支，使用包含 `DDSCL_FULLSCREEN | DDSCL_EXCLUSIVE` 的协作级别；为假时跳到 `0x00405EA4` 的窗口分支。该分支会以窗口样式计算外框、调用 `MoveWindow`，再建立窗口化 DirectDraw 表面。

窗口化后备表面创建没有显式填写 `DDPIXELFORMAT`，全屏时它依赖显示模式自然得到 16 位格式；窗口化后 Windows 桌面通常是 32 位，于是游戏把 16 位像素写入 32 位表面，出现半宽、紫色噪声。启动器在 `sub_405B30` 的 `CreateSurface` 调用前补齐 16 位 RGB 565 描述（`R=F800h`、`G=07E0h`、`B=001Fh`），让 DirectDraw 在复制到桌面主表面时执行正确的格式转换。

这里有两个容易混淆的结构偏移：`DDSURFACEDESC2.dwFlags` 位于描述符 `+0x04`，必须包含 `DDSD_PIXELFORMAT (0x1000)`；内嵌的 `DDPIXELFORMAT` 从 `+0x48` 开始，其中 `dwSize`、`dwFlags`、`dwRGBBitCount`、三个颜色掩码分别位于 `+0x48`、`+0x4C`、`+0x54`、`+0x58`、`+0x5C`、`+0x60`。此前测试版本误写了 `+0x44` 等位置，DirectDraw 因未看到有效的像素格式描述而继续按桌面色深解释，导致画面只显示约半宽并出现紫色噪声；当前版本已改为上述偏移，并显式 OR `0x1000`。钩子通过 `PUSHAD` 保存原始 `ECX`（描述符指针），从保存区 `[ESP+0x18]` 取回该指针。

显示函数 `sub_4064E0` 再次确认窗口化不是猜测：当同一标志为假时，它调用 `GetClientRect`、两次 `ClientToScreen`，然后以客户区矩形为目标调用 `IDirectDrawSurface::Blt`。源矩形来自游戏的 640 × 480 渲染面，因此目标客户区更大时会由 DirectDraw 拉伸。

启动器使用 2 处运行时补丁：

| VA | 原始 | 替换 | 效果 |
| --- | --- | --- | --- |
| `0x0040183A` | `push 0x80000000` | `push 0x00CA0000` | 将 `WS_POPUP` 改为带标题栏、系统菜单和最小化按钮的不可手动缩放窗口。 |
| `0x00405C5B` | `mov al, [esi+30h]; test al, al` | `xor al, al; mov [esi+30h], al` | 初始化时清除渲染对象的全屏标志。原有的后续判断因此自然进入窗口化 DirectDraw、表面恢复和 `Blt` 路径，不再调用全屏专用的 `Flip`。 |

### 鼠标坐标与倍率

原游戏的多个输入函数直接调用 `GetCursorPos`，并把返回的屏幕坐标当作全屏 640 × 480 坐标。例如 `sub_404600` 直接以 `0x1F4`、`0x24E` 等屏幕 X 坐标作菜单命中测试。仅将窗口化渲染面放大必然造成窗口边框偏移和倍率偏移。

因此，窗口模式开启时，启动器会在新进程中分配少量 x86 代码，并替换游戏导入表中的 `GetCursorPos` 指针。代码执行顺序为：

1. 调用原始 `GetCursorPos`。
2. 读取游戏窗口句柄 `0x0046F384`，在游戏进程内部解析 `USER32!ScreenToClient`。
3. 直接将屏幕坐标转换为游戏客户区坐标。
4. 分别除以 1、2 或 3，写回 `POINT`。

钩子还保留了 `GetCursorPos` 的 `BOOL` 返回约定：坐标转换成功时返回 `TRUE`；原始 API 失败时保留其失败值；窗口句柄或 `ScreenToClient` 解析/调用失败时清零 `POINT` 并返回 `FALSE`，避免把未经转换的桌面坐标误交给游戏。

这使游戏始终收到与其 640 × 480 逻辑坐标一致的输入。窗口可以移动，但不允许拖拽缩放；倍率只由启动器中的下拉框设置，避免客户区尺寸和输入换算脱节。坐标转换使用 `ScreenToClient`，而不是通过 `ClientToScreen(0,0)` 间接计算窗口原点；这与游戏自绘鼠标使用的客户区坐标系一致。

## 运行时安全措施

- 目标为新建且仍处于挂起状态的游戏进程。
- 每个选中组在写入前均以 `ReadProcessMemory` 比对原始字节。
- 任何位点不匹配、远程内存分配失败或写入失败都会终止该新进程。
- 完成写入后才调用 `ResumeThread`。
- 不会向磁盘上的 `RPG.exe` 写入任何字节。
