# 宽屏渲染补丁计划

## 目标

在不修改原始 `RPG.exe` 文件的前提下，为 Castle 游戏增加宽屏渲染支持：

1. 地图场景扩大横向可见区域，显示角色两侧原本视口之外的地图内容，而不是拉伸原来的 `640x480` 画面。
2. 菜单、战斗、对话、结算和视频等不适合扩大视野的界面保持原始 `4:3` 布局，在宽屏窗口中居中显示。
3. 固定 `4:3` 内容两侧使用黑边，或使用经过限制的地图背景模糊效果填充。
4. 鼠标坐标、镜头边界、地图对象裁剪和场景切换与新的呈现区域保持一致。

本计划同时记录当前第一版实现和后续验证步骤。宽屏选项、运行时尺寸调整、地图镜头扩展、固定界面居中呈现和输入坐标转换已经写入源码；实际游戏行为仍需人工验证。

### 当前实现状态

- launcher 已增加 `Display/Widescreen` 选项，并要求宽屏模式与窗口模式同时启用。
- runtime 在初始化尺寸链上将地图内部宽度设为 `864`，高度保持 `480`，并扩大普通地图的水平镜头范围。
- runtime 在地图入口记录当前帧类型；地图帧使用完整宽屏呈现，其他场景从 `640x480` 源区域居中呈现并填充黑边。
- 窗口模式下的鼠标坐标在地图帧和固定界面帧之间使用对应的正向、逆向转换。
- 尚未启动游戏验证地图边缘、场景切换、Bink、窗口缩放、surface 丢失恢复和鼠标命中；这些项目仍属于人工验收范围。

## 已确认的基础

### 主循环与呈现

```text
_WinMain@16 0x00401650
  -> sub_44A970 0x0044A970
     -> sub_405A10 0x00405A10
        -> sub_406330 0x00406330
        -> sub_4064E0 0x004064E0
```

`sub_4064E0` 是最终的 `Flip`/窗口化 `Blt` 入口。只修改它的目标客户区会产生缩放，不会增加地图视野。

### 地图绘制

```text
sub_40B050 0x0040B050
  -> sub_44B500       镜头抖动
  -> sub_407020       特殊地图效果
     或 sub_40ADE0    地图对象遍历
  -> sub_406DA0       附加层
  -> sub_44B590       还原镜头抖动
  -> sub_403E30       地图状态和过渡
  -> sub_404800       地图主体绘制
  -> sub_408810       后置地图层
```

主体地图层继续经过：

```text
sub_404800
  -> sub_407510
     -> sub_407270
     -> sub_4075E0 或 sub_407AC0
        -> 地块循环
        -> sub_44E677/sub_44E2BA/sub_44CBD3 等像素复制函数
```

地图对象继续经过：

```text
sub_40ADE0
  -> sub_409BE0/sub_409C50
     -> sub_407F70       世界坐标减 camera_x/camera_y
     -> sub_4068A0       对象资源绘制
     -> sub_4080D0
        -> sub_408320    对象矩形可见性判断
```

### 镜头和地图边界

`sub_44B360` 是普通镜头跟随函数。当前水平方向使用：

```text
0x100
0x180
```

两者合计 `0x280`，对应原始 `640` 像素水平视口。

`0x00978514` 和 `0x00978518` 是当前镜头坐标。`0x0097851C`、`0x00978520` 等变量由 `sub_44B300` 从地图矩形写入，表示地图边界，不能当作屏幕宽高替换。

### 内部渲染面

内部渲染尺寸链为：

```text
DDDes.Sci
  -> sub_4014E0
     -> 渲染对象尺寸
  -> sub_405960
     -> CPU 后备缓冲
  -> sub_405BD0/sub_405B30
     -> DirectDraw surface
  -> sub_4064E0
     -> 窗口或全屏呈现
```

`sub_405960` 和 `sub_405BD0` 会使用渲染对象中的宽高，因此宽屏方案应优先围绕这条尺寸链设计，而不是修改地图文件或全局替换 `0x280/0x1E0`。

## 总体方案

宽屏呈现分为两个独立模式：

```text
地图模式
  使用扩展后的内部渲染宽度
  扩大地图横向镜头和地块绘制范围
  提交完整宽屏地图画面

固定界面模式
  使用原始 640x480 逻辑画面
  在宽屏客户区中央计算 4:3 目标矩形
  两侧填充黑边或地图背景
```

两种模式共享窗口和最终呈现基础，但不共享地图视口参数。

## 阶段一：建立场景分类

### 目的

在修改任何绘制参数前，确认每一帧属于地图模式还是固定界面模式。

### 工作内容

1. 继续使用 IDA Pro 9.4 无界面分析 `dword_46F390`、`dword_46F640`、`byte_46F678`、`byte_46F679` 等状态变量的写入和读取关系。
2. 核对 `sub_40B050` 所属对象和其他场景对象的虚表布局。
3. 确认以下场景的进入、绘制和退出路径：
   - 普通地图；
   - `MP3001` 大地图；
   - 菜单和子菜单；
   - 普通战斗；
   - 战斗结算；
   - 对话和事件界面；
   - Bink 视频；
   - 地图切换和过渡画面。
4. 为运行时补丁定义最小场景判定条件。优先复用游戏已有状态，不新增复杂的状态机。

### 验收条件

- 地图绘制只在地图模式启用。
- 菜单、战斗、对话、结算和 Bink 不会误用地图宽屏视口。
- 场景切换前后模式不会残留。

## 阶段二：扩大地图内部渲染面

### 目标尺寸

第一版先使用一个固定、可验证的内部宽度，建议从 `864x480` 开始。高度保持 `480`，避免同时引入垂直视口变化。

最终宽度应满足以下条件：

- DirectDraw surface 创建成功；
- CPU 后备缓冲 pitch 正确；
- 地图地块步长 `0x40/0x30` 不需要改变；
- 不会覆盖固定资源或 Bink 使用的内存布局；
- 地图场景能够完整提交新增的横向区域。

### 需要处理的路径

1. 在 `sub_4014E0` 的尺寸读取链上确定宽度字段，并只在地图宽屏模式使用扩展值。
2. 检查 `sub_405960` 的每行 pitch、缓冲区大小和清理范围。
3. 检查 `sub_405BD0/sub_405B30` 的 surface 描述符和创建宽高。
4. 确认渲染对象中保存的宽度、高度、pitch 和像素缓冲地址在所有调用者中保持一致。
5. 保持菜单、战斗、视频等固定场景使用原始内部尺寸，或在进入这些场景时使用独立的 `640x480` 绘制区域。

### 禁止做法

- 不修改磁盘上的 `DDDes.Sci`。
- 不全局替换所有 `0x280`、`0x1E0`、`0x240`、`0x300`。
- 不修改地图文件的记录大小或地块数据。
- 不让 Bink 复制路径使用地图宽度。

## 阶段三：扩大地图视野

### 镜头跟随

修改 `sub_44B360` 的水平跟随逻辑，使其使用新的水平视口中心和边界。

原始逻辑可以抽象为：

```text
player_x 接近 camera_x + 0x100 时向右移动镜头
player_x 接近 camera_x + 0x280 时继续限制镜头
```

宽屏版本应根据新的内部宽度重新计算左右边界，保持角色位于合理的视口区域。不能只把 `0x180` 替换成更大的值，否则会造成镜头偏移和单侧扩展。

同时保留：

- 地图最小边界；
- 地图最大边界；
- 地图切换时的镜头重置；
- 镜头抖动前后的坐标恢复；
- 垂直方向的原始跟随逻辑。

### 地块绘制

核对并适配：

```text
sub_407510
sub_4075E0
sub_407AC0
sub_407270
sub_408810 -> sub_40A6D0 -> sub_4074A0
```

地块大小继续保持：

```text
横向 0x40
纵向 0x30
```

只修改绘制范围和目标边界，使地块循环覆盖扩展后的内部画布。地图边界判断必须仍然以地图数据边界为准，不能为了填满宽屏而读取地图数组之外的数据。

### 地图对象

核对并适配：

```text
sub_40ADE0
sub_409BE0
sub_409C50
sub_407F70
sub_4080D0
sub_408320
sub_408440
```

对象坐标仍使用：

```text
screen_x = world_x - camera_x
screen_y = world_y - camera_y
```

`sub_408320` 的矩形相交判断应接收扩展后的目标矩形。若调用者仍传入固定 `640x480` 边界，则在调用者处修改，不重写底层矩形判断算法。

### 地图过渡和后置层

地图主体之外还要覆盖：

- `sub_403E30` 的地图状态过渡；
- `sub_406DA0` 的附加效果；
- `sub_408810` 的后置地图层；
- 地图切换时 `sub_44B360` 的镜头重置；
- 地图对象排序、遮挡和障碍物绘制。

## 阶段四：固定界面居中呈现

### 适用场景

以下场景默认保持原始 `4:3` 逻辑布局：

- 主菜单和子菜单；
- 战斗界面；
- 战斗结算；
- 对话和事件界面；
- 固定图片或过渡画面；
- Bink 视频。

这些场景不应因为地图视口扩大而改变按钮、文本、战斗单位或视频的逻辑坐标。

### 目标矩形

在宽屏客户区中计算保持 `4:3` 的居中矩形：

```text
target_height = client_height
target_width  = target_height * 4 / 3
target_x      = (client_width - target_width) / 2
target_y      = 0
```

如果客户区比例小于 `4:3`，则改为以宽度为基准计算高度。

固定界面只把原始 `640x480` 源画面缩放到该目标矩形，不允许拉伸到完整宽屏客户区。

### 黑边

黑边是第一选择：

1. 在呈现前清理整个客户区为黑色。
2. 将固定 `4:3` 源画面复制到中央目标矩形。
3. 地图模式不使用此填充逻辑，直接提交完整地图画面。

黑边方案不需要新增图像资源，失败时也容易回退。

### 地图背景模糊化

地图模糊化作为第二种填充模式，不与地图扩大视野混用。

固定界面进入时：

1. 保存最近一张有效地图画面，或在切换前复制地图帧。
2. 将地图画面缩放到左右侧边区域。
3. 对侧边区域执行低成本模糊处理。
4. 在中央绘制固定 `4:3` 界面。
5. 地图帧不可用、切换过快、surface 丢失或复制失败时退回纯黑边。

模糊背景不得覆盖中央界面，也不得成为新的游戏输入区域。侧边只属于视觉填充。

### 模糊实现限制

- 不新增渲染线程。
- 不跨线程共享 DirectDraw surface。
- 不改变游戏逻辑帧率。
- 不让模糊缓冲参与地图碰撞、鼠标命中或脚本逻辑。
- 优先使用已有 CPU 像素缓冲和少量临时内存。
- 如果模糊导致场景切换、视频播放或退出不稳定，保留黑边作为默认实现。

## 阶段五：输入坐标

### 地图模式

地图模式下，鼠标应映射到完整的宽屏地图客户区：

```text
client_x/y -> 地图内部坐标
```

必须继续通过 `ScreenToClient` 获取客户区坐标，不能直接使用屏幕坐标。

### 固定界面模式

固定界面下，鼠标坐标需要减去中央 `4:3` 目标矩形的偏移，再按目标缩放比例还原为 `640x480` 逻辑坐标：

```text
logical_x = (client_x - target_x) * 640 / target_width
logical_y = (client_y - target_y) * 480 / target_height
```

点击两侧填充区域时，不应触发游戏界面命中。`SetCursorPos` 也必须使用同一套逆变换。

## 阶段六：补丁安全机制

所有运行时补丁继续遵守项目现有约束：

- 只修改新建且仍处于挂起状态的目标进程；
- 不写入原始 `RPG.exe`；
- 写入前校验原始字节；
- 代码分配和可写数据分开；
- 跳转位移按实际地址计算；
- 任一 patch site、缓冲区或 surface 初始化失败时终止目标进程或回退到原始路径；
- 不通过隐藏崩溃来掩盖补丁错误。

## 验证计划

### 静态验证

使用 IDA Pro 9.4 无界面脚本确认：

1. 渲染尺寸字段的所有读取和写入；
2. `sub_44B360` 的全部水平边界分支；
3. `sub_4075E0`、`sub_407AC0` 的全部循环出口；
4. `sub_4080D0 -> sub_408320` 的矩形参数来源；
5. 主体地图层和后置地图层的所有入口；
6. 固定界面、战斗和 Bink 是否经过地图宽屏条件；
7. 输入函数是否存在未经过 `GetCursorPos` IAT 的独立坐标读取。

### 运行时验证

不自动启动游戏。人工测试时至少覆盖：

- 普通地图横向移动；
- `MP3001` 大地图横向移动；
- 地图边缘和地图角落；
- 主角、NPC、特效和遮挡对象位于新增侧边区域；
- 地图切换和传送；
- 菜单进入和退出；
- 普通战斗与战斗结算；
- 对话和事件界面；
- Bink 播放、暂停、结束和返回游戏；
- 最小化、恢复和窗口移动；
- 鼠标点击中央界面和两侧填充区域；
- surface 丢失和恢复。

### 地图宽屏通过条件

- 地图两侧显示真实地图内容，而不是原画面拉伸；
- 镜头跟随保持合理中心位置；
- 地图边缘不读取越界数据；
- 新增区域中的人物、NPC、特效和遮挡关系正确；
- 地图输入和鼠标点击位置正确；
- 菜单、战斗、对话和视频不受地图视口参数污染。

### 固定界面通过条件

- 所有固定界面保持 `4:3` 比例；
- 内容在宽屏客户区水平居中；
- 黑边或模糊背景不遮挡中央内容；
- 两侧填充区域不响应游戏输入；
- 模糊缓冲不可用时稳定退回黑边；
- 场景切换、视频播放、最小化恢复和退出稳定。

## 分阶段交付

### P0：证据和场景判定

- 完成尺寸字段、场景状态和对象矩形参数的最终静态证据；
- 明确地图模式与固定界面模式的判定条件；
- 不改变游戏行为。

### P1：地图宽屏核心

- 扩大地图内部渲染宽度；
- 修改水平镜头跟随范围；
- 覆盖主体层、后置层和地图对象；
- 让地图模式提交完整宽屏画面；
- 完成地图边缘和输入验证。

### P2：固定界面居中和黑边

- 固定界面中央 `4:3` 呈现；
- 黑边填充；
- 输入坐标适配；
- 完成菜单、战斗、对话、结算和视频验证。

### P3：地图背景模糊化

- 保存可用地图帧；
- 实现左右侧边填充和模糊；
- 失败自动回退黑边；
- 在不影响 P1/P2 稳定性的情况下作为可选效果交付。

## 明确不纳入本计划

- 修改地图数据文件；
- 改变地图碰撞、移动速度或地图逻辑；
- 改变战斗逻辑或战斗单位布局；
- Bink 视频内容重编码；
- 渲染线程重构；
- 逻辑帧率或动画插值；
- 自动启动游戏进行验证。
