# 帧率与逻辑更新分离可行性分析

## 结论

当前证据**不能证明 20 帧由 DirectDraw 或固定 `Blt` 限制造成**。已经确认的是：游戏把逻辑绘制和呈现放在同一条调用链上，窗口模式的呈现函数每次只复制一次 640 × 480 的后备画面。

在不改变游戏逻辑速度的前提下，存在两种不同难度的目标：

| 目标 | 可行性 | 说明 |
| --- | --- | --- |
| 20 Hz 逻辑，60 Hz 重复显示最近完成的画面 | 低 | 如果每个逻辑帧已经持续到下一帧，这在 60 Hz 显示器上本来就是每帧保持 3 次刷新；额外重复 `Blt` 不会增加时间分辨率，也不会让动作更顺。只有存在撕裂或呈现节拍异常时才可能有收益。 |
| 20 Hz 逻辑，60 Hz 且动画状态连续 | 较低 | 需要把更新、绘制和输入从主循环中拆开，或为所有可动画对象增加插值/独立动画时钟。现有证据不足以支持直接打补丁。 |

因此第一阶段不应把“独立呈现最近完成帧”当作高帧率方案。应先验证当前 20 Hz 是否已经按 60 Hz 刷新节奏稳定保持画面，以及卡顿是否来自撕裂、阻塞或呈现节拍异常；不应直接把原有 DirectDraw 表面从逻辑线程移交给新线程。

## 已确认的调用链

`analysis/func_405A10.txt` 显示：

```text
sub_405A10
  -> sub_406330       恢复表面、锁定/绘制后备画面
  -> sub_4064E0       呈现
```

`analysis/ida_flip_calls.txt` 显示 `sub_4064E0` 的行为：

- 全屏分支调用 DirectDraw Surface 的 `Flip`。
- 窗口分支读取游戏窗口客户区，把客户区坐标转换为屏幕坐标，然后调用 Surface 虚表偏移 `+0x14` 的 `Blt`。
- 窗口分支没有等待、计时器或帧率上限逻辑。

`analysis/ida_render_flags.txt` 显示 `sub_406330` 在每次绘制前处理表面恢复和后备表面锁定；它也根据 `+0x30` 选择全屏/窗口绘制路径。当前窗口补丁只是让这个标志为假，并没有改变调用频率。

所以，若 20 帧是由固定节拍导致，固定节拍应位于上述调用链的更外层；若 20 帧是因为 `sub_406330` 前后的绘制工作耗时，单独增加 `Blt` 线程不能提高不同画面帧的生成速度。

## P0：现有垂直同步检查

当前静态证据暂时不支持“窗口模式已经做了垂直同步”：

- `sub_4064E0` 的全屏分支调用 `Flip` 时传入的 flags 为 `1`。按 DirectDraw SDK 定义，这对应 `DDFLIP_DONOTWAIT`，会绕过默认的等待行为；它不是 `DDFLIP_NOVSYNC`，但同样说明原全屏路径没有采用阻塞式的默认同步方式。`DDFLIP_NOVSYNC` 仍需检查是否由其他路径使用。
- 窗口分支调用 Surface 的 `Blt`，反汇编中没有发现 `WaitForVerticalBlank` 或其他显式垂直消隐等待。
- 窗口分支传入的 `0x01000000` 按 DirectDraw SDK 定义对应 `DDBLT_WAIT`。它只要求 bitblt 硬件忙时等待到可以提交，不是等待显示器垂直消隐；因此不能把现有 `Blt` 等待误判为 VSync。
- 当前仓库没有来自目标游戏运行时的 `Blt` 返回码、等待耗时、DWM 状态和显示器刷新率记录，因此还不能仅凭静态分析断言撕裂发生在哪一层。

P0 的第一步结论是：**窗口路径没有显式垂直同步；已有的 `DDBLT_WAIT` 只同步 bitblt 提交，不能防止扫描输出期间的撕裂。全屏路径使用了 `DDFLIP_DONOTWAIT`，也没有采用默认的阻塞式 Flip。** 在加入任何等待前，仍应补一次运行时测量，确认 `sub_4064E0` 的调用节奏、`Blt` 实际阻塞时长以及最终显示路径。

### P0 实测结果

已收集窗口模式下的连续诊断数据。主要结果如下：

- 大多数统计窗口的平均呈现间隔为 `49.99` 至 `50.36 ms`，确认呈现调用约为 20 Hz；
- 呈现函数平均耗时约 `0.20` 至 `0.32 ms`，最大值通常低于 `1.0 ms`，偶发最大值约 `1.9 ms`；
- 多数稳定窗口的最小/最大间隔约为 `47` 至 `53 ms`，说明没有接近 16.7 ms 的 60 Hz 呈现节奏；
- 部分窗口出现 `4` 至 `11 ms` 的短间隔并伴随约 `100` 至 `115 ms` 的长间隔，符合调度抖动、逻辑帧延迟后补发或其他主循环节奏异常，不符合 `Blt` 等待垂直消隐的特征；
- 首个统计窗口包含启动/切换阶段的长停顿，不能用于判断稳定运行状态。

因此可以排除当前 `DDBLT_WAIT` 在呈现线程中隐式等待 VSync：它的耗时只有亚毫秒级，且调用节奏仍由 50 ms 的逻辑循环决定。P0 还不能仅凭 CPU 侧日志证明屏幕上一定发生了撕裂；但如果人工观察确实存在撕裂，那么问题应位于窗口化 DirectDraw surface 提交之后的扫描/桌面合成时序，而不是 `Blt` 调用本身耗时不足。

### VRR 环境限制

当前显示器支持动态刷新率时，肉眼结果不能作为可靠判据：VRR 可能改变扫描时刻，DWM 可能继续合成窗口，20 Hz 画面的保持/顿挫也可能被误认为撕裂。P0 的人工复现应至少在一个固定环境中进行：关闭显示器 VRR、Windows 动态刷新率和驱动自适应同步，固定为 60 Hz，并保持窗口模式与缩放设置不变。

若固定环境下仍无法稳定观察撕裂，应使用高帧率屏幕录制或显示器 OSD/采集卡逐帧检查，而不是凭偶发肉眼感受推进补丁。反过来，若只在 VRR 开启时出现或消失，应把它记录为显示链路兼容性差异，不能归因于游戏 `Blt` 是否等待。

### 全屏 P1 实测结果

全屏诊断环境为 DWM 开启、`3440x1440`、`60 Hz`、`32 bpp`。连续数据表明：

- 稳定统计窗口的平均呈现间隔仍约为 `49.97` 至 `50.38 ms`，全屏没有获得额外呈现频率；
- `sub_4064E0` 平均耗时约为 `0.20` 至 `0.42 ms`，绝大多数最大值低于 `1.5 ms`；
- 少数窗口最大值约 `3` 至 `6.4 ms`，仍不是稳定的 `16.7 ms` 垂直消隐等待；
- 启动阶段的 `72.6 ms` 统计窗口包含初始化停顿，不能作为稳定帧节奏依据。

这与全屏 `Flip` 传入 `DDFLIP_DONOTWAIT` 的静态证据一致：当前全屏路径没有让逻辑线程等待一个完整的显示周期。P1-F 没有发现有效的阻塞式同步，因此不能作为撕裂修复完成的依据；下一步应单独对全屏 `Flip` 进行最小的默认等待实验，窗口路径保持不变。

P2-F 已实现为默认关闭的进程内补丁：配置 `[Display] FullscreenVSync=1` 后，在 `0x004064ED` 建立跳板，在全屏 `Flip` 前调用 DirectDraw 接口的 `WaitForVerticalBlank(DDWAITVB_BLOCKBEGIN, NULL)`，再执行原有 `Flip`。该补丁只作用于全屏分支；窗口模式不会写入此地址。

### 窗口化路径的 VSync 状态

当前窗口化补丁没有显式启用垂直同步：`sub_405BD0` 在窗口分支使用 `DDSCL_NORMAL`，`sub_4064E0` 使用 `Blt` 的 `DDBLT_WAIT (0x01000000)`。后者只等待 bitblt 可以提交，不等待显示器垂直消隐。P2-F2 的跳板位于全屏分支 `0x004064ED`，窗口分支不会执行它。

因此窗口模式目前依赖 Windows DWM 对窗口内容的合成时序，而不是补丁自身的 VSync。DWM 通常会把窗口更新纳入桌面合成，但这不等于 DirectDraw `Blt` 已经与显示器垂直消隐同步；是否撕裂仍取决于系统、驱动、DWM 状态和兼容层。

窗口路径后续应单独评估 `DwmFlush` 或独立呈现线程。直接在逻辑线程的 `Blt` 前等待会拖慢 20 Hz 逻辑；若要保持逻辑节奏，应优先研究已完成画面的独立呈现路径。

### P2-F 实测结果

开启 `FullscreenVSync=1` 后，全屏稳定统计窗口仍主要表现为：

- 呈现间隔约 `49.97` 至 `50.30 ms`；
- 呈现平均耗时约 `0.23` 至 `0.42 ms`；
- 最大耗时多数低于 `1.5 ms`，偶发约 `3` 至 `9.6 ms`；
- 没有出现稳定接近 `16.7 ms` 的阻塞。

因此将 `DDFLIP_DONOTWAIT` 改为默认 `Flip` 参数没有产生可观测的 VSync 等待。可能原因是现代 DirectDraw 兼容层异步排队了翻转，或该路径不再由旧式独占硬件翻转语义执行。该结果不足以证明物理扫描一定撕裂，但可以判定这项最小 P2-F 补丁没有提供可测的同步效果。

## 方案评估

### 诊断方向：呈现前等待垂直消隐

如果主要问题是撕裂，最小补丁不是增加渲染线程，而是在窗口化 `Blt` 前建立垂直同步。逻辑仍按原来的 20 Hz 运行，每次完成画面后等待下一次垂直消隐，再提交这一帧。这样只能消除一帧内部上下部分来自不同画面的撕裂，不能消除 20 Hz 固有的运动顿挫。

但当前游戏把逻辑和呈现放在同一线程，因此这个方案不能直接作为最终实现。20 Hz 的逻辑周期约为 50 ms；若呈现时等待 60 Hz 垂直消隐，最坏可能额外阻塞接近 16.7 ms，平均也可能增加数毫秒。该延迟会推迟下一次逻辑更新，并影响输入采样、计时器和事件处理。

需要先确认以下实现路径：

- DirectDraw 对象是否能通过已取得的接口调用 `WaitForVerticalBlank`，以及该接口在窗口化协作级别下是否实际生效。
- 当前 `Blt` 的目标 surface 是 primary surface 还是由 DirectDraw/DWM 接管的窗口 surface。
- 现代 Windows 桌面合成是否已经负责最终同步；若是，撕裂可能来自更早的 surface 更新、兼容层或窗口呈现路径，而不是缺少一次 `WaitForVerticalBlank`。
- 等待应放在 `Blt` 前，并测量阻塞时间；不能把等待放到逻辑更新前，否则会把显示同步延迟传导回游戏逻辑。

推荐先做诊断性原型：只在窗口模式的 `sub_4064E0` 路径记录 `Blt` 调用时间、返回时间和显示模式，不改变逻辑；再对比加入垂直消隐等待后的撕裂情况。如果等待接口在窗口模式无效，或确认它会拖慢逻辑，就不能继续沿用同线程等待。

最终方向应是逻辑线程完成一张完整画面后，以非阻塞方式发布快照；独立呈现线程负责等待垂直消隐和提交快照。这样逻辑线程只承担一次快照复制或交换指针的成本，不等待显示器。该方案仍不会生成新的逻辑帧，只解决提交时序问题；代价是需要独立的双缓冲/三缓冲、场景切换同步和退出协议。

### 方案 A：后备表面快照 + 呈现线程

这是风险最低的实验方案：

1. 在 `sub_405A10` 返回前，仍由原线程完成所有游戏绘制。
2. 将完成的 640 × 480 画面复制到一个独立的系统内存 DirectDraw surface，或复制到自有 16 位像素缓冲区。
3. 独立线程按 60 Hz 调用窗口化 `Blt`，始终只读取“上一张完整快照”。
4. 使用双缓冲或序号协议发布快照，不能让呈现线程读取正在写入的缓冲区。

优点是不会让游戏对象、DirectDraw 后备表面和渲染上下文跨线程共享。缺点是同一逻辑帧会被重复显示 2 至 3 次，动画仍然是 20 Hz；在正常 60 Hz 显示输出下，这通常与显示器自然保持画面没有区别，复制 640 × 480 × 2 字节的快照反而增加 CPU 带宽成本。

### 方案 B：只把 `Blt` 移到线程

不建议。原 `sub_4064E0` 直接读取游戏的 DirectDraw surface；而 `sub_406330` 可能正在恢复、锁定或解锁同一组 surface。没有确认 DirectDraw 对这些对象的跨线程调用约束，也没有确认所有生命周期路径都已停止使用这些对象。该方案可能造成撕裂、`DDERR_SURFACEBUSY`、设备恢复竞态或退出时悬空访问。

### 方案 C：主循环固定更新、独立绘制

只有在确认绘制函数不会修改游戏状态后才考虑。经典游戏经常在绘制过程中更新动画计数器、事件队列、视频帧或对象可见性；把绘制搬到另一个线程会引入大量未锁定的全局状态访问。若必须实现，应先把“纯绘制入口”和“状态更新入口”分别识别出来，再设计只读快照，而不是给整个游戏状态加一个粗粒度锁。

### 方案 D：逻辑帧之间做渲染插值

这是画面质量最高但工作量最大的方案。需要保存前后两个逻辑状态，并对角色坐标、镜头、特效等字段插值；碰撞、脚本、输入和随机数仍按原逻辑节奏运行。现阶段没有对象结构、动画时钟和主循环证据，不应作为第一版补丁目标。

## 必须补齐的逆向证据

下一轮 IDA 分析应围绕以下问题取证：

1. 找到 `sub_405A10` 的全部调用方，向上追到游戏主循环，记录消息泵、跳转回边界和每次循环调用的更新函数。
2. 在主循环及其调用树中搜索 `Sleep`、`GetTickCount`、`timeGetTime`、`QueryPerformanceCounter`、`PeekMessage`、`GetMessage` 和 `WaitMessage` 的 IAT 引用。
3. 确认 `sub_405A10` 是否只在主循环调用，还是也被场景切换、视频播放和表面恢复路径调用。
4. 记录 `sub_406330` 是否调用 `Lock`/`Unlock`，以及后备 surface 的真实地址、pitch 和生命周期。
5. 对 `sub_405A10` 前后的全局写入做数据流标记，特别是动画计数器、镜头坐标、对象链表、输入状态和脚本事件队列。
6. 在运行时仅做诊断统计：记录逻辑入口、`sub_406330`、`sub_4064E0` 的调用频率和耗时，确认 20 Hz 的来源。不要先改写主循环或跨线程调用 DirectDraw。

## 推荐验证顺序

1. 先用原版和窗口补丁分别统计上述三个函数的频率，建立基线。
2. 若 `sub_4064E0` 与逻辑入口同频且存在明确等待，验证“跳过等待但不增加逻辑更新”是否能提高呈现机会；这一步可能只暴露撕裂，不一定产生更高质量画面。
3. 若绘制已完成但呈现受限，原型化方案 A，只重复显示完整快照。
4. 只有方案 A 能改善观感、且退出/切换场景稳定，才继续评估真正的渲染与逻辑分离。

## 当前判断

推荐把该需求拆为“修复 20 Hz 呈现节拍/撕裂”和“在逻辑 20 Hz 下增加独立视觉变化”两个独立需求。前者只有在测量确认存在呈现问题时才值得做；后者若不改变游戏逻辑，只能针对不影响逻辑结果的视觉内容单独建立时钟或缓存，不能通过简单提高主循环频率实现。
